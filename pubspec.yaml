name: simple_exercise_builder
description: App for building simple exercises for beginners.

publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  carousel_slider: ^4.0.0
  cupertino_icons: ^1.0.2
  event_bloc: ^1.0.0+1
  event_navigation: ^0.1.2
  hive: ^2.0.4
  provider: ^5.0.0
  search_me_up: ^0.1.0
  shared_preferences: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
